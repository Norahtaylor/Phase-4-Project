<!--<!DOCTYPE html>
<html>
 <body>
    <%= yield %>
    <%# templates cooresponding to our controller actions get rendered  %>

    <br>

    <% if logged_in? %>
      <%= button_to "Logout", sessions_path, method: :delete %>
    <% else %>
      <%= button_to "Login Page", login_path, method: :get %>
    <% end %>
  </body>
</html>